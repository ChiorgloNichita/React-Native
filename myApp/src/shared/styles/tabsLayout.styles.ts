import { StyleSheet } from "react-native";

export const tabsLayoutStyles = StyleSheet.create({
  tabBar: {
    backgroundColor: "#fff",
    borderTopWidth: 0.5,
    borderTopColor: "#ddd",
    paddingBottom: 6,
    height: 60,
  },
});
